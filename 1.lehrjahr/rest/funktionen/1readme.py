#! /usr/bin/python3
# EIne FUnktion hat einen ein und einen Ausgang
# EIne FUnktion löst sich zu ihrem Rückgabe Wert auf
'''
Funktionen

FUnktionen haben einen Namen. Nach dem Namen folgen runde Klammern.
In dn Runden KLammern können Parameter stehen. Meherere Parameter werden
durch Komma getrennt

DUe PArameter in den Klammern bilden den Funktionseingang

Das Ergebnis der Funktion wird an die Aufrufsstelle geliefert.
Der Aufruf löst sich zum Ergebniswert auf.

für Aufrufer ist
laenge =  len("Hallo")
leange = 5

Eine FUnktion ist ein Programmstück, das anderen Programmstücken
dient. Es kann jederzeit von oihnen benutzt werden.

FUnktionen reduzieren Redundanz, indem sich wiederholte Programmeile
in Funktionen zusammengefasst werden
'''

def addiere(wert_eins, wert_zwei):
    wert=wert_eins + wert_zwei
    print(wert)

addiere(1,2)
